domain: qufe.net       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://www.qufe.net  # the base hostname and protocol for your site, e.g. http://example.com
baseurl: /qufe/      # place folder name if the site is served in a subfolder
